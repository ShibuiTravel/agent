name: Build Binaries Disabled

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Disabled
        run: |
          echo "The inherited Pi binary workflow is disabled until Shibui owns binary naming, checksums, signing, and release recovery."
